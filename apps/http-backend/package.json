{
  "name": "http-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "pnpm --filter @repo/db run build && tsc -b --force",
    "dev": "pnpm run build && pnpm run start",
    "start": "node ./dist/index.js"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@types/jsonwebtoken": "^9.0.10",
    "bcryptjs": "^3.0.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "@repo/backend-common": "workspace:*",
    "@repo/common": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/express": "^5.0.6"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
